interface:
  display_name: Subagent Lane Planner
  short_description: Split decision, disjoint scopes, dependency order, and child prompts for subagent lanes.
  default_prompt: Decide whether subagent lanes help; plan only when scopes are clear and disjoint.
